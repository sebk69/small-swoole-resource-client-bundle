#!/bin/bash

if [ "$(docker ps | grep small-swoole-resource-client-bundle)" == "" ]
then
  docker compose up -d --build
fi

docker exec -it small-swoole-resource-client-bundle composer $1 $2 $3 $4 $5 $6 $7 $8 $9